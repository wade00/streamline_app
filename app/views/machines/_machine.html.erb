<% if add_outdated_banner(machine) %>
  <tr class="machine-preview machine-preview-outdated" id="<%= dom_id machine %>">
<% else %>
  <tr class="machine-preview" id="<%= dom_id machine %>">
<% end %>
  <%= render 'machine_content', machine: machine %>
</tr>

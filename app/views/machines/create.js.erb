if (<%= @machine.save %>) {
  var listingsForm = $('<%= j(render 'form', machine: @machine) %>');
  listingsForm.hide();

  $('.edit-form-container').html('');
  $('.edit-form-container').html(listingsForm);
  listingsForm.fadeIn();
}

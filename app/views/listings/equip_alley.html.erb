<div class="table-container-narrow listing-site-container">
  <div class="page-header">
    <h3 class="page-header-headline">Update these listings...</h3>
  </div>
  <ul class="listing-list">
    <% if @equip_alley_listings.count == 0 %>
      <h4 class="text-center">All listings up to date!</h4>
    <% else %>
      <%= render @equip_alley_listings %>
    <% end %>
  </ul>
</div>
<div class="listing-site-frame">
  <iframe src="https://www.equipmentalley.com/main/user/login">
    <p>Your browser does not support iframes.</p>
  </iframe>
</div>

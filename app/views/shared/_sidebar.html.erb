<div class="sidebar">
  <nav class="nav">
    <a href="#"><%= icon('flag') %></a>
    <a href="#"><%= icon('plus') %></a>
    <a href="#"><%= icon('user') %></a>
    <a  href="#" class="signout"><%= icon('sign-out') %></a>
  </nav>
</div>

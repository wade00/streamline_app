<nav class="nav">
  <%= menu(class: "sidebar") do |m| %>
    <%= m.inventory machines_path do %>
      <div><%= icon("th-list") %></div>
      <small>My Inventory</small>
    <% end %>

    <%= m.listings listings_path do %>
      <div>
        <span class="badge outdated-listing-count">
          <%= render 'layouts/outdated_listings_badge' %>
        </span>
      </div>
      <small>Outdated Listings</small>
    <% end %>

    <%= m.profile edit_user_registration_path do %>
      <div><%= icon("user") %></div>
      <small>My Profile</small>
    <% end %>

    <%= m.sign_out destroy_user_session_path, method: "delete", class: "sign-out" do %>
      <div><%= icon("sign-out") %></div>
      <small>Sign Out</small>
    <% end %>
  <% end %>
</nav>
